using System;
using UnityEngine;

[RequireComponent(typeof(NPCBehaviour))]
public class NPCAggro : MonoBehaviour
{
    /*private NPCBehaviour _behaviour;
    private Transform _target;
    
    private void Awake()
    {
        _behaviour = GetComponent<NPCBehaviour>();
        _target = GameObject.FindWithTag("Player").transform;
    }

    private void OnTriggerEnter(Collider other)
    {
        if (other.CompareTag("Player"))
        {
           
        }
    }

    private void OnTriggerExit(Collider other)
    {
        // Check if the collider belongs to a target (player, enemy, etc.)
        if (other.CompareTag("Player"))
        {
            // Handle target leaving (e.g., stop aggroing the NPC)
            // Example: AggroManager.LoseAggro(transform.position, other.transform.position);
        }
    }
    
    private void AggroRange()
    {
        
    }

    private void LineOfSight()
    {
        
    }

    private void Threat()
    {
        
    }*/
}
